#!/usr/bin/expect -f

set timeout 20
set ip "91.99.232.126"
set user "root"
set password "LpXqTEPurwUu"

spawn ssh -o StrictHostKeyChecking=no $user@$ip

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "# " {
    }
}

send "cat /var/www/whatsapp-bot-builder/products/ai-whatsapp-bot-builder/frontend/app/api/knowledge/upload/route.ts\r"
expect "# "

# Find where pdfjs is used using grep
send "grep -r \"pdfjs\" /var/www/whatsapp-bot-builder/products/ai-whatsapp-bot-builder/frontend/lib\r"
expect "# "

# Also check package.json for pdfjs-dist version
send "grep \"pdfjs-dist\" /var/www/whatsapp-bot-builder/products/ai-whatsapp-bot-builder/frontend/package.json\r"
expect "# "

send "exit\r"
expect eof

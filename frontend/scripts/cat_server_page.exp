#!/usr/bin/expect -f

set timeout 20
set ip "91.99.232.126"
set user "root"
set password "LpXqTEPurwUu"

spawn ssh -o StrictHostKeyChecking=no $user@$ip

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "# " {
    }
}

send "cat /var/www/whatsapp-bot-builder/products/ai-whatsapp-bot-builder/frontend/app/\\\[locale\\\]/page.tsx\r"
expect "# "

send "exit\r"
expect eof

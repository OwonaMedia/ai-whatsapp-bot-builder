#!/usr/bin/expect -f

set timeout 20
set ip "91.99.232.126"
set user "root"
set password "LpXqTEPurwUu"

spawn ssh -o StrictHostKeyChecking=no $user@$ip

expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    "# " {
    }
}

send "sed -n '300,600p' /var/www/whatsapp-bot-builder/products/ai-whatsapp-bot-builder/frontend/components/demo/RAGDemo.tsx\r"
expect "# "

send "ls -R /var/www/whatsapp-bot-builder/products/ai-whatsapp-bot-builder/frontend/app/api/knowledge\r"
expect "# "

send "exit\r"
expect eof
